The WHERE clause is used to filter records.
The WHERE clause is used to extract only those records that fulfill a specific condition.

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    marks INT
);

INSERT INTO students (name, email, marks) VALUES
('Raju', 'raju@gmail.com', 85),
('Amit', 'amit@gmail.com', 65),
('Sohan', 'sohan@gmail.com', 90),
('Priya', 'priya@gmail.com', 70);


////order by

The ORDER BY keyword is used to sort the result-set in ascending or descending order.

The ORDER BY keyword sorts the records in ascending order (ASC) by default.
SELECT * FROM student
ORDER BY Price DESC;